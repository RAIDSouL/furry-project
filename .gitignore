# Godot 4+ specific ignores (per Godot version-control best practices)
.godot/

# Godot-specific ignores
.import/
export.cfg
# export_presets.cfg can hold secrets (e.g. Android keystore passwords).
# Remove this line if you have no secrets and want to share export presets.
export_presets.cfg

# Imported translations (auto-generated from CSV files)
*.translation

# Mono / C# specific ignores
.mono/
data_*/
mono_crash.*.json

# Android build template
/android/

# Local tooling (GitHub CLI download, not part of the project)
gh-cli/
gh.zip
